import styled from 'styled-components';

export const Header = styled.header`
  display: flex;
  align-items: center;
  height: 50px;
  gap: 30px;

  & > select {
    width: 300px;
    height: 100%;
  }
`;
